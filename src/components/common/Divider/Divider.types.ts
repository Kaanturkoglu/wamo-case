export interface DividerProps {
  orientation?: "horizontal" | "vertical";
  color?: string;
  opacity?: number; 
  thickness?: number; 
  length?: string; 
  style?: React.CSSProperties; 
  margin?: string;
}